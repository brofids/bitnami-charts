{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.query.enabled .Values.query.httproute.enabled -}}
{{- if or (not .Values.query.httproute.parentRefs) (lt (len .Values.query.httproute.parentRefs) 1) }}
{{- fail "query.httproute.parentRefs must be defined and contain at least one Gateway reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "thanos.query.fullname" . }}
  namespace: {{ include "common.names.namespace" . }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.query.httproute.labels .Values.commonLabels ) "context" . ) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: query
  {{- if or .Values.query.httproute.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.query.httproute.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- range $idx, $parentRef := .Values.query.httproute.parentRefs }}
    - group: {{ default "gateway.networking.k8s.io" $parentRef.group }}
      kind: {{ default "Gateway" $parentRef.kind }}
      name: {{ include "common.tplvalues.render" ( dict "value" (required (printf "query.httproute.parentRefs[%d].name is required" $idx) $parentRef.name) "context" $ ) }}
      {{- if $parentRef.namespace }}
      namespace: {{ include "common.tplvalues.render" ( dict "value" $parentRef.namespace "context" $ ) }}
      {{- end }}
      {{- if $parentRef.sectionName }}
      sectionName: {{ include "common.tplvalues.render" ( dict "value" $parentRef.sectionName "context" $ ) }}
      {{- end }}
      {{- if $parentRef.port }}
      port: {{ $parentRef.port }}
      {{- end }}
    {{- end }}
  {{- if .Values.query.httproute.hostnames }}
  hostnames: {{- include "common.tplvalues.render" ( dict "value" .Values.query.httproute.hostnames "context" $ ) | nindent 4 }}
  {{- else if or .Values.query.ingress.hostname .Values.query.ingress.extraHosts }}
  hostnames:
    {{- if .Values.query.ingress.hostname }}
    - {{ include "common.tplvalues.render" ( dict "value" .Values.query.ingress.hostname "context" $ ) | quote }}
    {{- end }}
    {{- range .Values.query.ingress.extraHosts }}
    - {{ include "common.tplvalues.render" ( dict "value" .name "context" $ ) | quote }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.query.httproute.rules }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.query.httproute.rules "context" $ ) | nindent 4 }}
    {{- else }}
    - matches:
        {{- if .Values.query.httproute.matches }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.query.httproute.matches "context" $ ) | nindent 8 }}
        {{- else }}
        - path:
            {{- $pathType := default .Values.query.ingress.pathType .Values.query.httproute.pathType }}
            {{- if eq $pathType "Exact" }}
            type: Exact
            {{- else if eq $pathType "RegularExpression" }}
            type: RegularExpression
            {{- else }}
            type: PathPrefix
            {{- end }}
            value: {{ default .Values.query.ingress.path .Values.query.httproute.path | quote }}
        {{- end }}
      {{- if .Values.query.httproute.filters }}
      filters: {{- include "common.tplvalues.render" ( dict "value" .Values.query.httproute.filters "context" $ ) | nindent 8 }}
      {{- end }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "thanos.query.fullname" . }}
          port: {{ .Values.query.service.ports.http }}
          weight: 1
      {{- if .Values.query.httproute.timeouts }}
      timeouts: {{- include "common.tplvalues.render" ( dict "value" .Values.query.httproute.timeouts "context" $ ) | nindent 8 }}
      {{- end }}
    {{- end }}
{{- end }}
