{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.ruler.enabled .Values.ruler.httproute.enabled -}}
{{- if or (not .Values.ruler.httproute.parentRefs) (lt (len .Values.ruler.httproute.parentRefs) 1) }}
{{- fail "ruler.httproute.parentRefs must be defined and contain at least one Gateway reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "thanos.ruler.fullname" . }}
  namespace: {{ include "common.names.namespace" . }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.ruler.httproute.labels .Values.commonLabels ) "context" . ) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: ruler
  {{- if or .Values.ruler.httproute.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.ruler.httproute.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- range $idx, $parentRef := .Values.ruler.httproute.parentRefs }}
    - group: {{ default "gateway.networking.k8s.io" $parentRef.group }}
      kind: {{ default "Gateway" $parentRef.kind }}
      name: {{ include "common.tplvalues.render" ( dict "value" (required (printf "ruler.httproute.parentRefs[%d].name is required" $idx) $parentRef.name) "context" $ ) }}
      {{- if $parentRef.namespace }}
      namespace: {{ include "common.tplvalues.render" ( dict "value" $parentRef.namespace "context" $ ) }}
      {{- end }}
      {{- if $parentRef.sectionName }}
      sectionName: {{ include "common.tplvalues.render" ( dict "value" $parentRef.sectionName "context" $ ) }}
      {{- end }}
      {{- if $parentRef.port }}
      port: {{ $parentRef.port }}
      {{- end }}
    {{- end }}
  {{- if .Values.ruler.httproute.hostnames }}
  hostnames: {{- include "common.tplvalues.render" ( dict "value" .Values.ruler.httproute.hostnames "context" $ ) | nindent 4 }}
  {{- else if or .Values.ruler.ingress.hostname .Values.ruler.ingress.extraHosts }}
  hostnames:
    {{- if .Values.ruler.ingress.hostname }}
    - {{ include "common.tplvalues.render" ( dict "value" .Values.ruler.ingress.hostname "context" $ ) | quote }}
    {{- end }}
    {{- range .Values.ruler.ingress.extraHosts }}
    - {{ include "common.tplvalues.render" ( dict "value" .name "context" $ ) | quote }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.ruler.httproute.rules }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.ruler.httproute.rules "context" $ ) | nindent 4 }}
    {{- else }}
    - matches:
        {{- if .Values.ruler.httproute.matches }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.ruler.httproute.matches "context" $ ) | nindent 8 }}
        {{- else }}
        - path:
            {{- $pathType := default .Values.ruler.ingress.pathType .Values.ruler.httproute.pathType }}
            {{- if eq $pathType "Exact" }}
            type: Exact
            {{- else if eq $pathType "RegularExpression" }}
            type: RegularExpression
            {{- else }}
            type: PathPrefix
            {{- end }}
            value: {{ default .Values.ruler.ingress.path .Values.ruler.httproute.path | quote }}
        {{- end }}
      {{- if .Values.ruler.httproute.filters }}
      filters: {{- include "common.tplvalues.render" ( dict "value" .Values.ruler.httproute.filters "context" $ ) | nindent 8 }}
      {{- end }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "thanos.ruler.fullname" . }}
          port: {{ .Values.ruler.service.ports.http }}
          weight: 1
      {{- if .Values.ruler.httproute.timeouts }}
      timeouts: {{- include "common.tplvalues.render" ( dict "value" .Values.ruler.httproute.timeouts "context" $ ) | nindent 8 }}
      {{- end }}
    {{- end }}
{{- end }}
