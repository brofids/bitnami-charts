{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.queryFrontend.enabled .Values.queryFrontend.httproute.enabled -}}
{{- if or (not .Values.queryFrontend.httproute.parentRefs) (lt (len .Values.queryFrontend.httproute.parentRefs) 1) }}
{{- fail "queryFrontend.httproute.parentRefs must be defined and contain at least one Gateway reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "thanos.query-frontend.fullname" . }}
  namespace: {{ include "common.names.namespace" . }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.queryFrontend.httproute.labels .Values.commonLabels ) "context" . ) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: query-frontend
  {{- if or .Values.queryFrontend.httproute.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.queryFrontend.httproute.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- range $idx, $parentRef := .Values.queryFrontend.httproute.parentRefs }}
    - group: {{ default "gateway.networking.k8s.io" $parentRef.group }}
      kind: {{ default "Gateway" $parentRef.kind }}
      name: {{ include "common.tplvalues.render" ( dict "value" (required (printf "queryFrontend.httproute.parentRefs[%d].name is required" $idx) $parentRef.name) "context" $ ) }}
      {{- if $parentRef.namespace }}
      namespace: {{ include "common.tplvalues.render" ( dict "value" $parentRef.namespace "context" $ ) }}
      {{- end }}
      {{- if $parentRef.sectionName }}
      sectionName: {{ include "common.tplvalues.render" ( dict "value" $parentRef.sectionName "context" $ ) }}
      {{- end }}
      {{- if $parentRef.port }}
      port: {{ $parentRef.port }}
      {{- end }}
    {{- end }}
  {{- if .Values.queryFrontend.httproute.hostnames }}
  hostnames: {{- include "common.tplvalues.render" ( dict "value" .Values.queryFrontend.httproute.hostnames "context" $ ) | nindent 4 }}
  {{- else if or .Values.queryFrontend.ingress.hostname .Values.queryFrontend.ingress.extraHosts }}
  hostnames:
    {{- if .Values.queryFrontend.ingress.hostname }}
    - {{ include "common.tplvalues.render" ( dict "value" .Values.queryFrontend.ingress.hostname "context" $ ) | quote }}
    {{- end }}
    {{- range .Values.queryFrontend.ingress.extraHosts }}
    - {{ include "common.tplvalues.render" ( dict "value" .name "context" $ ) | quote }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.queryFrontend.httproute.rules }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.queryFrontend.httproute.rules "context" $ ) | nindent 4 }}
    {{- else }}
    - matches:
        {{- if .Values.queryFrontend.httproute.matches }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.queryFrontend.httproute.matches "context" $ ) | nindent 8 }}
        {{- else }}
        - path:
            {{- $pathType := default .Values.queryFrontend.ingress.pathType .Values.queryFrontend.httproute.pathType }}
            {{- if eq $pathType "Exact" }}
            type: Exact
            {{- else if eq $pathType "RegularExpression" }}
            type: RegularExpression
            {{- else }}
            type: PathPrefix
            {{- end }}
            value: {{ default .Values.queryFrontend.ingress.path .Values.queryFrontend.httproute.path | quote }}
        {{- end }}
      {{- if .Values.queryFrontend.httproute.filters }}
      filters: {{- include "common.tplvalues.render" ( dict "value" .Values.queryFrontend.httproute.filters "context" $ ) | nindent 8 }}
      {{- end }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "thanos.query-frontend.fullname" . }}
          port: {{ .Values.queryFrontend.service.ports.http }}
          weight: 1
      {{- if .Values.queryFrontend.httproute.timeouts }}
      timeouts: {{- include "common.tplvalues.render" ( dict "value" .Values.queryFrontend.httproute.timeouts "context" $ ) | nindent 8 }}
      {{- end }}
    {{- end }}
{{- end }}
