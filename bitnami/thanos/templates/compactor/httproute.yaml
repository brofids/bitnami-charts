{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.compactor.enabled .Values.compactor.httproute.enabled -}}
{{- if or (not .Values.compactor.httproute.parentRefs) (lt (len .Values.compactor.httproute.parentRefs) 1) }}
{{- fail "compactor.httproute.parentRefs must be defined and contain at least one Gateway reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "thanos.compactor.fullname" . }}
  namespace: {{ include "common.names.namespace" . }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.compactor.httproute.labels .Values.commonLabels ) "context" . ) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: compactor
  {{- if or .Values.compactor.httproute.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.compactor.httproute.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- range $idx, $parentRef := .Values.compactor.httproute.parentRefs }}
    - group: {{ default "gateway.networking.k8s.io" $parentRef.group }}
      kind: {{ default "Gateway" $parentRef.kind }}
      name: {{ include "common.tplvalues.render" ( dict "value" (required (printf "compactor.httproute.parentRefs[%d].name is required" $idx) $parentRef.name) "context" $ ) }}
      {{- if $parentRef.namespace }}
      namespace: {{ include "common.tplvalues.render" ( dict "value" $parentRef.namespace "context" $ ) }}
      {{- end }}
      {{- if $parentRef.sectionName }}
      sectionName: {{ include "common.tplvalues.render" ( dict "value" $parentRef.sectionName "context" $ ) }}
      {{- end }}
      {{- if $parentRef.port }}
      port: {{ $parentRef.port }}
      {{- end }}
    {{- end }}
  {{- if .Values.compactor.httproute.hostnames }}
  hostnames: {{- include "common.tplvalues.render" ( dict "value" .Values.compactor.httproute.hostnames "context" $ ) | nindent 4 }}
  {{- else if or .Values.compactor.ingress.hostname .Values.compactor.ingress.extraHosts }}
  hostnames:
    {{- if .Values.compactor.ingress.hostname }}
    - {{ include "common.tplvalues.render" ( dict "value" .Values.compactor.ingress.hostname "context" $ ) | quote }}
    {{- end }}
    {{- range .Values.compactor.ingress.extraHosts }}
    - {{ include "common.tplvalues.render" ( dict "value" .name "context" $ ) | quote }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.compactor.httproute.rules }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.compactor.httproute.rules "context" $ ) | nindent 4 }}
    {{- else }}
    - matches:
        {{- if .Values.compactor.httproute.matches }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.compactor.httproute.matches "context" $ ) | nindent 8 }}
        {{- else }}
        - path:
            {{- $pathType := default .Values.compactor.ingress.pathType .Values.compactor.httproute.pathType }}
            {{- if eq $pathType "Exact" }}
            type: Exact
            {{- else if eq $pathType "RegularExpression" }}
            type: RegularExpression
            {{- else }}
            type: PathPrefix
            {{- end }}
            value: {{ default .Values.compactor.ingress.path .Values.compactor.httproute.path | quote }}
        {{- end }}
      {{- if .Values.compactor.httproute.filters }}
      filters: {{- include "common.tplvalues.render" ( dict "value" .Values.compactor.httproute.filters "context" $ ) | nindent 8 }}
      {{- end }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "thanos.compactor.fullname" . }}
          port: {{ .Values.compactor.service.ports.http }}
          weight: 1
      {{- if .Values.compactor.httproute.timeouts }}
      timeouts: {{- include "common.tplvalues.render" ( dict "value" .Values.compactor.httproute.timeouts "context" $ ) | nindent 8 }}
      {{- end }}
    {{- end }}
{{- end }}
