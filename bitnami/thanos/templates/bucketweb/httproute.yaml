{{- /*
Copyright Broadcom, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.bucketweb.enabled .Values.bucketweb.httproute.enabled -}}
{{- if or (not .Values.bucketweb.httproute.parentRefs) (lt (len .Values.bucketweb.httproute.parentRefs) 1) }}
{{- fail "bucketweb.httproute.parentRefs must be defined and contain at least one Gateway reference" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "thanos.bucketweb.fullname" . }}
  namespace: {{ include "common.names.namespace" . }}
  {{- $labels := include "common.tplvalues.merge" ( dict "values" ( list .Values.bucketweb.httproute.labels .Values.commonLabels ) "context" . ) }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" $labels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: bucketweb
  {{- if or .Values.bucketweb.httproute.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" ( dict "values" ( list .Values.bucketweb.httproute.annotations .Values.commonAnnotations ) "context" . ) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $) | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- range $idx, $parentRef := .Values.bucketweb.httproute.parentRefs }}
    - group: {{ default "gateway.networking.k8s.io" $parentRef.group }}
      kind: {{ default "Gateway" $parentRef.kind }}
      name: {{ include "common.tplvalues.render" ( dict "value" (required (printf "bucketweb.httproute.parentRefs[%d].name is required" $idx) $parentRef.name) "context" $ ) }}
      {{- if $parentRef.namespace }}
      namespace: {{ include "common.tplvalues.render" ( dict "value" $parentRef.namespace "context" $ ) }}
      {{- end }}
      {{- if $parentRef.sectionName }}
      sectionName: {{ include "common.tplvalues.render" ( dict "value" $parentRef.sectionName "context" $ ) }}
      {{- end }}
      {{- if $parentRef.port }}
      port: {{ $parentRef.port }}
      {{- end }}
    {{- end }}
  {{- if .Values.bucketweb.httproute.hostnames }}
  hostnames: {{- include "common.tplvalues.render" ( dict "value" .Values.bucketweb.httproute.hostnames "context" $ ) | nindent 4 }}
  {{- else if or .Values.bucketweb.ingress.hostname .Values.bucketweb.ingress.extraHosts }}
  hostnames:
    {{- if .Values.bucketweb.ingress.hostname }}
    - {{ include "common.tplvalues.render" ( dict "value" .Values.bucketweb.ingress.hostname "context" $ ) | quote }}
    {{- end }}
    {{- range .Values.bucketweb.ingress.extraHosts }}
    - {{ include "common.tplvalues.render" ( dict "value" .name "context" $ ) | quote }}
    {{- end }}
  {{- end }}
  rules:
    {{- if .Values.bucketweb.httproute.rules }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.bucketweb.httproute.rules "context" $ ) | nindent 4 }}
    {{- else }}
    - matches:
        {{- if .Values.bucketweb.httproute.matches }}
        {{- include "common.tplvalues.render" ( dict "value" .Values.bucketweb.httproute.matches "context" $ ) | nindent 8 }}
        {{- else }}
        - path:
            {{- $pathType := default .Values.bucketweb.ingress.pathType .Values.bucketweb.httproute.pathType }}
            {{- if eq $pathType "Exact" }}
            type: Exact
            {{- else if eq $pathType "RegularExpression" }}
            type: RegularExpression
            {{- else }}
            type: PathPrefix
            {{- end }}
            value: {{ default .Values.bucketweb.ingress.path .Values.bucketweb.httproute.path | quote }}
        {{- end }}
      {{- if .Values.bucketweb.httproute.filters }}
      filters: {{- include "common.tplvalues.render" ( dict "value" .Values.bucketweb.httproute.filters "context" $ ) | nindent 8 }}
      {{- end }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "thanos.bucketweb.fullname" . }}
          port: {{ .Values.bucketweb.service.ports.http }}
          weight: 1
      {{- if .Values.bucketweb.httproute.timeouts }}
      timeouts: {{- include "common.tplvalues.render" ( dict "value" .Values.bucketweb.httproute.timeouts "context" $ ) | nindent 8 }}
      {{- end }}
    {{- end }}
{{- end }}
